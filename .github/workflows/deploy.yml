name: Deploy

# Le staging suit automatiquement une CI réussie ; la production reste manuelle.
on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: Environnement cible
        required: true
        default: production
        type: choice
        options:
          - production
          - staging

permissions:
  # Le workflow lit le dépôt, mais ne le modifie pas.
  contents: read

# Empêche deux déploiements simultanés vers le même environnement.
concurrency:
  group: deploy-${{ github.event_name == 'workflow_run' && 'staging' || inputs.environment }}
  cancel-in-progress: false

jobs:
  # Vérifie les prérequis avant toute tentative de connexion distante.
  preflight:
    name: Vérifier la configuration du déploiement
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    environment: ${{ github.event_name == 'workflow_run' && 'staging' || inputs.environment }}
    outputs:
      configured: ${{ steps.check.outputs.configured }}
    steps:
      - name: Vérifier les secrets de déploiement
        id: check
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        run: |
          set -euo pipefail
          missing=0

          for secret_name in DEPLOY_HOST DEPLOY_USER DEPLOY_SSH_KEY DEPLOY_PATH; do
            if [ -z "${!secret_name}" ]; then
              echo "::warning title=Missing deployment secret::${secret_name} is not configured."
              missing=1
            fi
          done

          if [ "$missing" -ne 0 ]; then
            echo "configured=false" >> "$GITHUB_OUTPUT"

            if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
              echo "::error title=Deployment unavailable::Configure DEPLOY_HOST, DEPLOY_USER, DEPLOY_SSH_KEY and DEPLOY_PATH before running a manual deployment."
              exit 1
            fi

            echo "::notice title=Deployment skipped::No deployment target is configured, so the automatic staging deployment is skipped."
            exit 0
          fi

          echo "configured=true" >> "$GITHUB_OUTPUT"

  # Déploiement SSH par mise à jour du dépôt et relance de Docker Compose.
  deploy:
    name: Déployer avec Docker Compose
    runs-on: ubuntu-latest
    needs: preflight
    if: ${{ needs.preflight.outputs.configured == 'true' }}
    environment: ${{ github.event_name == 'workflow_run' && 'staging' || inputs.environment }}
    steps:
      - name: Configurer SSH
        shell: bash
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts

      - name: Mettre à jour le serveur et redémarrer les services
        shell: bash
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/deploy_key "$DEPLOY_USER@$DEPLOY_HOST" "DEPLOY_PATH='$DEPLOY_PATH' bash -s" << 'EOF'
            set -e
            cd "$DEPLOY_PATH"
            git fetch origin main
            git checkout main
            git pull --ff-only origin main
            docker compose config
            # Attendre les healthchecks évite de déclarer un déploiement réussi trop tôt.
            docker compose up --build -d --remove-orphans --wait --wait-timeout 240
            docker compose ps
            docker compose logs --tail=100
            # Nettoyage limité aux images inutilisées.
            docker image prune -f
          EOF

      # La clé temporaire est supprimée même si le déploiement échoue.
      - name: Supprimer la clé SSH temporaire
        if: always()
        shell: bash
        run: rm -f ~/.ssh/deploy_key
